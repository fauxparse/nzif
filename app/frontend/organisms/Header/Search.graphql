query Search($query: String!) {
  search(query: $query) {
    id
    title
    description
    url

    ... on PageResult {
      lede
    }

    ... on ActivityResult {
      activity {
        id
        name
        type
      }
    }

    ... on UserResult {
      user {
        id
        name
        email
      }
    }

    ... on VenueResult {
      venue {
        id
        room
        building
        address
      }
    }
  }
}
